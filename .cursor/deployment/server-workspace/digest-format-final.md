# Формат дайджестов — Финальный формат для Telegram

Два типа дайджестов:

- **📰 Новостной** — факты, события, анонсы (08:00, 13:00, 18:00)
- **💬 Мнения** — комментарии, мысли, аналитика блогеров/экспертов (08:30, 13:30, 18:30)

Оба типа используют ОДИНАКОВЫЕ правила: формат, дедупликацию, валидацию свежести, нативные реакции.

---

# 📰 НОВОСТНОЙ ДАЙДЖЕСТ

## Заголовок дайджеста:

Первое сообщение дайджеста (ОБЯЗАТЕЛЬНО указывай время и модель!):

```
🌅 **УТРЕННИЙ ДАЙДЖЕСТ** | 08.02.2026, 08:00
📊 12 новостей | 🎯 Только самые важные
🤖 _Модель: [название модели]_
```

Где `[название модели]` — текущая модель из runtime (например: `Llama 3.3 70B (free)`, `Claude Sonnet 4.6`, `Claude Opus 4.6`).
Модель указывается в системных данных сессии.

Формат: `ДД.ММ.ГГГГ, ЧЧ:ММ` — время местное (по текущему timezone пользователя из MEMORY.md).

## Отправка каждой новости — АТОМАРНЫЙ ЦИКЛ (нельзя разрывать!):

> ⚠️ **Каждая новость = 2 обязательных шага подряд. Пропуск шага 2 — баг системы.**

### Шаг 1 — Отправить сообщение:

```
message(action=send, channel=telegram, target=@newsneiron, message="...")
```

→ Получи `messageId` из ответа.

### Шаг 2 — СРАЗУ сохранить message_id (до следующей новости!):

```
Прочитай data/dual-rating-data.json
Добавь в messageHistory:
  "msg_{messageId}": {
    "source": { "name": "<источник>", "url": "<url>" },
    "category": "<категория>",
    "title": "<заголовок>",
    "timestamp": <unix ms>
  }
Сохрани файл.
```

**Только после выполнения обоих шагов — переходи к следующей новости.**

**НЕ добавляй `buttons`!** Оценка идёт через нативные реакции Telegram.

---

## 💾 Детали сохранения message_id (справка):

После каждого `sendMessage` OpenClaw возвращает объект сообщения с `message_id`.
Ты **обязан** сохранить маппинг id → источник, иначе реакции не смогут обновить рейтинг.

Алгоритм:

1. Получи `message_id` из ответа на `sendMessage`
2. Прочитай `data/dual-rating-data.json`
3. В секцию `messageHistory` добавь запись:

```json
"msg_{message_id}": {
  "chatId": "tg:685668909",
  "source": { "name": "<название источника>", "url": "<url статьи>" },
  "category": "<категория: AI/Robotics/eVTOL/etc>",
  "title": "<заголовок новости>",
  "timestamp": <текущее время в unix ms>
}
```

4. Сохрани файл

> Если источник — эксперт (дайджест мнений), также заполни поле `"expert": { "name": "...", "handle": "@..." }`.

---

## Оценка через нативные реакции Telegram:

Пользователь ставит реакцию (emoji) прямо на сообщение — зажатием или двойным тапом.

Бот получает два типа событий реакций:

- **Из личного чата / группы** — `message_reaction` (содержит имя пользователя, например "Margulan")
- **Из канала @newsneiron** — `message_reaction_count` (анонимная агрегация, лейбл отправителя: "channel subscriber")

Оба типа приходят в **одинаковом формате**:

```
Telegram reaction added: 👍 by Margulan on msg 12345
Telegram reaction added: 🔥 by channel subscriber on msg 309
```

Алгоритм обработки идентичен для обоих случаев — лейбл отправителя не влияет на логику.

### Маппинг emoji → рейтинг:

| Реакция | Значение    | Баллы |
| ------- | ----------- | ----- |
| 🔥      | Отлично     | +10   |
| 👍      | Нравится    | +5    |
| 👎      | Не нравится | -3    |
| 💩      | Мусор       | -5    |

### Обработка реакции:

Ты получишь системное событие вида:

```
Telegram reaction added: 👍 by Margulan on msg 12345
```

**Детальный алгоритм:**

1. Извлеки `message_id` — число после `"msg "` (в примере: `12345`)
2. Определи `score` по таблице выше (👍 → +5, 🔥 → +10, и т.д.)
3. Если emoji не входит в таблицу (❤️, 😂 и т.д.) — **игнорируй**, не обновляй ничего, ответь `[SILENT]`
4. Прочитай `data/dual-rating-data.json`
5. Найди `messageHistory["msg_12345"]` → получи `source.name` (и `expert.name` если есть)
6. Если запись в `messageHistory` не найдена — реакция на не-дайджестное сообщение, игнорируй, ответь `[SILENT]`
7. Найди `sourceRatings[source.name]`. Если записи нет — создай:

```json
{
  "name": "<source.name>",
  "url": "<source.url>",
  "category": "<category>",
  "totalScore": 0,
  "reactionsCount": 0,
  "reactions": [],
  "averageScore": 0,
  "status": "candidate",
  "lastReaction": null
}
```

8. Добавь реакцию в `reactions[]`:

```json
{ "reaction": "👍", "score": 5, "userId": 685668909, "timestamp": <unix ms> }
```

9. Пересчитай агрегаты:
   - `totalScore += score`
   - `reactionsCount += 1`
   - `averageScore = totalScore / reactionsCount`
   - `lastReaction = <текущий unix ms>`
10. Обнови `status`:
    - `averageScore >= 7` → `"proven"`
    - `averageScore < -2` → `"rejected"`
    - иначе → `"candidate"`
11. Если есть `expert.name` — аналогично обнови `expertRatings[expert.name]`
12. Обнови `categoryRatings[category]` из `messageHistory` записи:
    - Получи `category` из `messageHistory["msg_12345"].category`
    - Найди `categoryRatings[category]`. Если нет — создай с нулями и статусом `"candidate"`
    - `totalScore += score`, `reactionsCount += 1`, `averageScore = totalScore / reactionsCount`
    - Обнови `status`: `averageScore >= 7` → `"proven"`, `< -2` → `"rejected"`, иначе `"candidate"`
13. Сохрани `data/dual-rating-data.json`
14. Ответь `[SILENT]` — никакого текстового ответа пользователю

## 📊 Отбор новостей — двухфазный алгоритм с защитой от пузырей:

Перед отбором прочитай `data/dual-rating-data.json`. Приоритет рейтингов:

```
Тема (50%) > Автор/Эксперт (30%) > Источник (20%)
```

Допустим, в дайджесте N новостей (обычно 12). Алгоритм:

---

### ФАЗА 1 — Гарантированные слоты (защита от информационных пузырей)

**Правило:** каждая из 7 тем получает **минимум 1 гарантированный слот**, даже если её рейтинг низкий или `"rejected"`.

Темы: ИИ, Вайбкодинг, Робототехника, eVTOL, Технологии, Бизнес, Инвестиции

Для каждой темы выбери **лучшую** доступную новость по `priorityScore`:

```
priorityScore = catScore*0.5 + expertScore*0.3 + srcScore*0.2
```

> ⚠️ **Важно:** статус `"rejected"` у темы означает "меньше приоритета", но НЕ "ноль слотов".
> Категория с `rejected` всё равно получает свой 1 гарантированный слот — это защита от однобокости.
> `"rejected"` применяется только к **источникам**: новость от `rejected` источника не попадает в дайджест.

После Фазы 1: 7 слотов заполнены (по одному от каждой темы).

---

### ФАЗА 2 — Бонусные слоты (персонализация)

Оставшиеся `N - 7` слотов распределяются по `priorityScore` среди всех оставшихся кандидатов.

**Анти-пузырь cap:** ни одна тема не может занять больше **40%** от итогового N.

- Пример: N=12 → максимум 4-5 новостей по одной теме, даже если ИИ имеет наивысший рейтинг.
- При достижении cap для темы — следующий бонусный слот идёт другой теме.

---

### ФАЗА 3 — Предпубликационная проверка редактора

**Перед отправкой** пройди по чеклисту. Если пункт не выполнен — скорректируй подборку:

**✅ Всесторонность:** все 7 тем представлены хотя бы 1 новостью?

- Если нет — замени аутсайдера другой новостью из отсутствующей темы (расширь поиск если нужно)

**✅ Баланс:** ни одна тема не занимает >40% дайджеста?

- Если нет — убери лишние новости доминирующей темы, добавь из недопредставленных

**✅ Качество:** средний `priorityScore` подборки > 0?

- Если нет (рейтинги ещё пустые) — отбирай по свежести и разнообразию тем

> Если рейтинги ещё пустые (первые дайджесты) — Фаза 2 отсутствует, Фаза 1 распределяет все N слотов равномерно по темам.

---

## Правила формата:

1. **Эмодзи + заголовок** — полная развёрнутая формулировка
2. **Теги сфер** — на отдельной строке после заголовка (из списка: ИИ, Вайбкодинг, Робототехника, eVTOL, Технологии, Бизнес, Инвестиции)
3. **Описание** — 3-5 полных предложений, развёрнутое с деталями и цифрами
4. **Источник + ссылка** — `📰 Название — url`
5. **Без inline кнопок** — оценка через нативные реакции

## ⏰ Валидация свежести (ОБЯЗАТЕЛЬНО!)

Каждая новость **ДОЛЖНА** пройти проверку свежести перед включением в дайджест:

### Правила свежести:

1. **Проверь дату публикации** — новость должна быть опубликована **не ранее 48 часов назад**
2. **При веб-поиске** используй параметр `freshness: "pd"` (past day) или `"pw"` (past week)
3. **Если дата неизвестна** — проверь содержание: упоминаются ли конкретные даты, "сегодня", "вчера"?
4. **Старые новости** (>48ч) → **ПРОПУСТИТЬ**, даже если они интересные

### Признаки устаревшей новости:

- Дата публикации >2 дней назад
- Событие уже широко обсуждается давно
- Пресс-релиз или анонс без свежей даты
- "Переписанная" версия старой новости с другого сайта

### Исключения (можно включить старше 48ч):

- ✅ **Развитие событий** — новый поворот в истории (пометь "Обновление:")
- ✅ **Эксклюзив** — ранее недоступная информация
- ✅ **Аналитика** — глубокий анализ свежего тренда

### Проверка при поиске:

```
web_search({ query: "...", freshness: "pd" })  // предпочтительно
web_search({ query: "...", freshness: "pw" })  // запасной вариант
```

## 🔁 Дедупликация (ОБЯЗАТЕЛЬНО!)

Перед каждым дайджестом:

1. **Прочитай** файл `data/sent-digests.json`
2. **Сравни** каждую найденную новость с записями за последние 7 дней:
   - Совпадение URL → **ПРОПУСТИТЬ** (точный дупликат)
   - Совпадение ключевых слов заголовка (≥3 слова совпадают) → **ПРОПУСТИТЬ** (вероятный дупликат)
   - Та же тема/событие от другого источника → **ПРОПУСТИТЬ** (семантический дупликат)
3. **После отправки** дайджеста — **ЗАПИШИ** все отправленные новости в `data/sent-digests.json`

### Формат записи в sent-digests.json:

```json
{
  "entries": [
    {
      "date": "2026-02-07",
      "digest": "morning",
      "title": "OpenAI анонсировала GPT-5",
      "url": "https://openai.com/gpt5",
      "source": "OpenAI Blog",
      "category": "AI",
      "keywords": ["openai", "gpt-5", "анонс", "мультимодальность"]
    }
  ]
}
```

### Правила дедупликации:

- **URL-матч** → 100% дупликат, ВСЕГДА пропускать
- **Заголовок-матч** (≥3 значимых слова) → пропускать
- **Семантический матч** (та же новость, другой источник) → пропускать
- **Развитие событий** → МОЖНО отправить, если есть НОВЫЕ факты (укажи "Обновление:")
- **Хранить записи** за последние 7 дней, старые удалять при записи

### Пример проверки:

Вчера отправлено: "OpenAI выпустила GPT-5"
Сегодня найдено:

- ❌ "OpenAI представила GPT-5" → дупликат (те же ключевые слова)
- ❌ "GPT-5 от OpenAI меняет рынок" → дупликат (та же тема)
- ✅ "GPT-5 получил сертификацию в ЕС" → новый факт, можно отправить

## ЗАПРЕЩЕНО (для обоих типов дайджестов):

- `<tg-spoiler>` или `||spoiler||`
- Короткие однострочные описания
- Итоговое сообщение после дайджеста
- Отправлять текстовый ответ при получении реакции (только [SILENT])
- Добавлять `buttons` / inline keyboard
- **Отправлять материалы, которые уже были в предыдущих дайджестах (за 7 дней)**
- **Отправлять материалы старше 48 часов** (кроме исключений выше)

---

# 💬 ДАЙДЖЕСТ МНЕНИЙ

## Заголовок дайджеста мнений:

Первое сообщение (ОБЯЗАТЕЛЬНО указывай время и модель!):

```
💬 **МНЕНИЯ И КОММЕНТАРИИ** | 08.02.2026, 08:30
📊 10 мнений | 🧠 Что думают эксперты
🤖 _Модель: [название модели]_
```

Где `[название модели]` — текущая модель из runtime.

## Что искать:

- **Посты блогеров** — Twitter/X, LinkedIn, Substack, Medium, Хабр
- **Мнения экспертов** — CEO, CTO, исследователи, аналитики
- **Комментарии известных людей** — инвесторы, предприниматели, учёные
- **Треды и обсуждения** — Reddit, HackerNews, Product Hunt
- **Подкасты/интервью** — ключевые цитаты и тейки

## Стартовые Telegram-каналы для мнений:

- **@serge_ai** — AI-эксперт
- **@alexkrol** — технологии и стратегия
- **@data_secrets** — data-аналитика
- **@andre_dataist** — data science

Все участвуют в рейтинговой системе наравне с остальными. Могут быть вытеснены более качественными источниками на основе реакций.

Полный список: `telegram-channels.md` → секция "Мнения & Эксперты"

## Источники (НЕ новостные!):

Ищи мнения, а НЕ новости. Разница:

- ❌ Новость: "OpenAI выпустила GPT-5" (факт)
- ✅ Мнение: "Андрей Карпатый: GPT-5 изменит подход к обучению ИИ" (тейк)

Типы источников:

- 📢 Telegram-каналы экспертов (приоритет!)
- 🐦 Twitter/X посты экспертов
- 📝 Блоги и Substack
- 💼 LinkedIn посты
- 🎙️ Подкасты (ключевые цитаты)
- 📊 Аналитические статьи
- 💬 Reddit/HN комментарии

## 📊 Отбор мнений — двухфазный алгоритм с защитой от пузырей:

Та же логика, что у новостного дайджеста:

```
Тема (50%) > Автор/Эксперт (30%) > Источник/Платформа (20%)
```

**ФАЗА 1:** каждая из 7 тем получает 1 гарантированный слот.
Для каждой темы выбери лучшего эксперта/мнение по `priorityScore`:

```
priorityScore = catScore*0.5 + expertScore*0.3 + srcScore*0.2
```

> Статус `"rejected"` для темы = меньше приоритета, но НЕ 0 слотов (защита от пузырей).
> Статус `"rejected"` для эксперта = этот конкретный человек не появляется.

**ФАЗА 2:** оставшиеся слоты по `priorityScore`. Cap: не более 40% мнений от одной темы.

**ФАЗА 3 — Предпубликационная проверка:**

- ✅ Все 7 тем представлены?
- ✅ Нет темы с долей >40%?
- ✅ Разнообразие авторов (не один и тот же эксперт во всех слотах)?

## Отправка каждого мнения — АТОМАРНЫЙ ЦИКЛ (нельзя разрывать!):

> ⚠️ **Каждое мнение = 2 обязательных шага подряд. Пропуск шага 2 — баг системы.**

### Шаг 1 — Отправить сообщение:

```
message(action=send, channel=telegram, target=@opnioneiron, message="...")
```

→ Получи `messageId` из ответа.

### Шаг 2 — СРАЗУ сохранить message_id (до следующего мнения!):

```
Прочитай data/dual-rating-data.json
Добавь в messageHistory:
  "msg_{messageId}": {
    "expert": { "name": "<имя>", "handle": "<@handle>" },
    "source": { "name": "<платформа>", "url": "<url>" },
    "category": "<категория>",
    "title": "<заголовок>",
    "timestamp": <unix ms>
  }
Сохрани файл.
```

**Реакция обновит оба рейтинга: expertRatings и sourceRatings.**
**Только после обоих шагов — переходи к следующему мнению.**

---

## Формат каждого мнения:

```json
{
  "channel": "telegram",
  "action": "sendMessage",
  "to": "tg:685668909",
  "content": "💬 **Андрей Карпатый о будущем вайбкодинга**\nИИ, Вайбкодинг\n\nКарпатый в своём посте на X написал, что через 2 года 80% кода будет генерироваться ИИ. Он считает, что роль разработчика сместится от написания кода к архитектуре и валидации. \"Мы переходим от программирования к супервизии\" — ключевая цитата.\n\n🐦 @karpathy — x.com/karpathy/status/123456"
}
```

## Правила формата мнений:

1. **Эмодзи + \"кто + о чём\"** — указывай автора и тему
2. **Теги сфер** — на отдельной строке (те же: ИИ, Вайбкодинг, Робототехника, eVTOL, Технологии, Бизнес, Инвестиции)
3. **Пересказ мнения** — 3-5 предложений, передай суть позиции автора
4. **Ключевая цитата** — если есть яркая фраза, выдели в кавычках
5. **Автор + ссылка** — `🐦 @автор — url` или `📝 Блог — url`
6. **Без inline кнопок** — оценка через нативные реакции

## Дедупликация мнений:

Используй тот же файл `data/sent-digests.json`, но с типом `"digest": "opinions"`.
Все правила дедупликации из раздела новостей применяются.

## Валидация свежести мнений:

Те же правила: не старше 48 часов, приоритет последним 24 часам.

---

# 🛡️ ОБЯЗАТЕЛЬНЫЙ АУДИТ (ДЛЯ ВСЕХ ДАЙДЖЕСТОВ)

Согласно правилу «Видимый Аудит» из `AGENTS.md`, каждый дайджест (и новостной, и мнений) должен завершаться блоком технической прозрачности.

**Где размещать:** отдельным последним сообщением после завершения всех атомарных циклов отправки новостей/мнений.

**Формат сообщения:**

```
🛡️ **АУДИТ ДАЙДЖЕСТА**
─────────────────────
👤 Автор: Нейрон (AI)
🤖 Модель: [название модели]
🧠 Мышление: [low/high]
📂 Файлы: digest-format-final.md, dual-rating-data.json, sent-digests.json ✅
⏱️ Статус: Дайджест сформирован и отправлен в @newsneiron
```

**Отправка:**
```
message(action=send, channel=telegram, target=@newsneiron, message="🛡️ **АУДИТ ДАЙДЖЕСТА**...")
```
(Для дайджеста мнений target=`@opnioneiron`)
