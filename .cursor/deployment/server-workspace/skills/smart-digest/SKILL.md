---
name: smart-digest
description: Умный дайджест — открывает URL через браузер, делает скриншоты, извлекает контент, генерирует саммари мнений авторов
command-description: Запустить Smart Digest для списка URL
user-invocable: yes
metadata: { "openclaw": { "requires": { "config": ["browser.enabled"] } } }
---

# Smart Digest

Автоматизированный пайплайн обработки статей. Принимает список URL, для каждого:

1. Открывает страницу в браузере
2. Делает скриншот
3. Извлекает основной контент (Main Content)
4. Генерирует саммари мнения автора

## Использование

```
/smart-digest url1 url2 url3 ...
```

Или пользователь передаёт URL в произвольном формате (по одному на строку, через запятую, или в сообщении).

## Пайплайн обработки (для каждого URL)

### Шаг 1: Подготовка

1. Прочитай `data/sent-digests.json` для дедупликации — пропусти URL, которые уже были отправлены за последние 7 дней
2. Запусти браузер если он не запущен: `browser` → `status`, если не active → `start`

### Шаг 2: Обработка каждого URL

Для каждого URL выполни последовательно:

#### 2a. Навигация

```json
{ "action": "navigate", "url": "<url>" }
```

Подожди полную загрузку страницы.

#### 2b. Скриншот

```json
{ "action": "screenshot" }
```

Сохрани скриншот — он пригодится для визуальной проверки контента и layout'а.

#### 2c. Извлечение контента

Используй **два метода** параллельно для лучшего результата:

**Метод A — Browser Snapshot:**

```json
{ "action": "snapshot", "type": "ai" }
```

Получи структурированное представление страницы.

**Метод B — Web Fetch (fallback):**

```json
web_fetch({"url": "<url>", "extractMode": "markdown", "maxChars": 30000})
```

Из полученного контента извлеки:

- **Заголовок статьи** (h1 или og:title)
- **Автор** (byline, author meta, подпись)
- **Дата публикации** (проверь свежесть: не старше 48 часов!)
- **Основной текст** (Main Content — тело статьи, без навигации/рекламы/сайдбаров)
- **Ключевые цитаты** автора (прямая речь в кавычках)

#### 2d. Анализ и саммари мнения автора

На основе извлечённого контента сгенерируй:

1. **Позиция автора** — что именно утверждает/предлагает автор? Какой его тейк?
2. **Ключевая цитата** — самая яркая фраза (дословно, в кавычках)
3. **Аргументация** — чем автор подкрепляет свою позицию (данные, примеры, логика)
4. **Контекст** — почему это мнение важно сейчас
5. **Категория** — ИИ, Вайбкодинг, Робототехника, eVTOL, Технологии, Бизнес, Инвестиции

### Шаг 3: Формирование дайджеста

Для каждого обработанного URL сформируй сообщение в формате дайджеста мнений:

```
💬 **[Автор] о [тема]**
[Теги категорий]

[Пересказ мнения автора 3-5 предложений. Передай суть позиции, не просто факты.
Включи ключевую цитату в кавычках.]

🔗 [Источник] — [url]
```

### Шаг 4: Отправка в Telegram

Отправь каждое мнение отдельным сообщением:

```json
{
  "channel": "telegram",
  "action": "sendMessage",
  "to": "tg:685668909",
  "content": "<сформированное сообщение>"
}
```

Перед отправкой — заголовок дайджеста:

```
💬 **SMART DIGEST** | ДД.ММ.ГГГГ, ЧЧ:ММ
📊 N мнений | 🧠 Глубокий анализ через браузер
```

### Шаг 4b: Сохранение message_id для реакций

После каждого `sendMessage` сохрани маппинг в `data/dual-rating-data.json` → `messageHistory`:

```json
"msg_{message_id}": {
  "chatId": "tg:685668909",
  "expert": { "name": "<автор>", "handle": "<@handle если есть>" },
  "source": { "name": "<издание/платформа>", "url": "<url>" },
  "category": "<категория>",
  "title": "<заголовок>",
  "timestamp": <unix ms>
}
```

Это позволит реакциям на сообщения обновлять рейтинг автора и источника.
Алгоритм обработки реакций — см. `digest-format-final.md` → секция "Обработка реакции".

### Шаг 4с: Обязательный аудит

В конце дайджеста отправь блок «🛡️ АУДИТ ДАЙДЖЕСТА» (см. `digest-format-final.md` → секция "ОБЯЗАТЕЛЬНЫЙ АУДИТ").

### Шаг 5: Сохранение результатов

После отправки запиши в `data/sent-digests.json`:

```json
{
  "date": "YYYY-MM-DD",
  "digest": "smart-digest",
  "title": "<заголовок>",
  "url": "<url>",
  "source": "<автор или издание>",
  "category": "<категория>",
  "keywords": ["ключевое1", "ключевое2", "ключевое3"]
}
```

## Обработка ошибок

- Если страница не загружается → попробуй `web_fetch` как fallback
- Если контент за paywall → отметь `[PAYWALL]` и извлеки что доступно из мета-тегов и preview
- Если нет явного автора → используй название издания
- Если статья старше 48 часов → предупреди пользователя, но обработай (это ручной запрос)

## Отличия от обычного дайджеста

| Обычный digest                | Smart Digest                   |
| ----------------------------- | ------------------------------ |
| Веб-поиск по ключевым словам  | Конкретные URL от пользователя |
| Поверхностное описание        | Глубокий анализ через браузер  |
| Без скриншотов                | Скриншот каждой страницы       |
| Пересказ заголовков           | Анализ позиции автора          |
| Автоматический запуск по cron | Ручной запуск по запросу       |
