---
name: uchastkovy
description: Участковый — мониторинг здоровья системы каждые 10 минут. Выявляет сбои крон-задач и системных сервисов. Докладывает Механику.
---

# Участковый 👮‍♂️

> «На моём участке — порядок и тишина. Если кто-то упал — я уже пишу рапорт.»

Скилл для регулярного мониторинга стабильности Нейрона. Работает в фоновом режиме.

## Обязанности (Checklist)

1. **Крон-задачи:** `cron(action=list)`.
   - Ищи задачи со статусом `error` или `skipped`.
   - Проверь `lastRunAtMs`. Если задача должна была запуститься, но не запустилась — это нарушение.
2. **Связь:** Проверка доступности Telegram и Mac-node (через `openclaw status`).
3. **Зависимости:** Проверка критических бинарников (summarize, yt-dlp, git).
4. **Git:** `git status`. Если есть незакоммиченные изменения — это риск.

## Логика рапорта
Если обнаружена проблема:
1. Запиши детали сбоя в `data/system_issues.jsonl` (это «журнал происшествий» для Механика).
2. Сформируй «рапорт» для Механика:
   ```markdown
   [РАПОРТ УЧАСТКОВОГО]
   Время: YYYY-MM-DD HH:MM
   Объект: [Название крон-задачи или сервиса]
   Суть: [Описание сбоя/ошибки]
   Срочность: [Критично / Средне]
   ```
3. НЕ пытайся чинить сам. Твоя задача — надзор и передача данных.

---
🛡️ **ОБЯЗАТЕЛЬНЫЙ АУДИТ:**
> **🛡️ Аудит Участкового:**
> - Участок проверен: [список систем] ✅
> - Происшествий зафиксировано: [число]
