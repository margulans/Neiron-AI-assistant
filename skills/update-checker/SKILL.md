---
name: update-checker
description: Проверка и установка обновлений OpenClaw с уведомлением в Telegram. Запускай по cron ежедневно и по триггерам «проверь обновления», «update check», «есть обновления?», «обнови openclaw», «update openclaw».
command-description: Проверить обновления OpenClaw
user-invocable: yes
metadata: { "openclaw": { "emoji": "🔄" } }
---

# Update Checker — Проверка и установка обновлений OpenClaw

Ежедневная проверка новых версий с уведомлением в Telegram и обновлением по команде.

## Когда запускать

1. **Cron** — ежедневно (рекомендуется 09:00 по местному времени)
2. **Вручную** — пользователь говорит «проверь обновления», «update check», «есть апдейт?»
3. **При /update-checker** — нативная команда

## Шаг 1: Проверка версии

Запусти скрипт:

```bash
bash skills/update-checker/scripts/check.sh
```

Скрипт вернёт JSON на stdout. Разбери результат:

- `"status":"up-to-date"` → перейди к Шагу 2a
- `"status":"update-available"` → перейди к Шагу 2b
- `"status":"error"` → отправь сообщение об ошибке и завершись

## Шаг 2a: Обновлений нет

Если запуск ручной (пользователь спросил) — отправь краткое подтверждение:

```
message send --to tg:685668909
✅ OpenClaw актуален — версия {current}
```

Если запуск по cron — **ничего не отправляй** (не спамить при отсутствии обновлений). Ответь `HEARTBEAT_OK` или просто завершись.

## Шаг 2b: Есть обновление — уведомление в Telegram

Проанализируй changelog из JSON-ответа. Подготовь сообщение:

### Формат уведомления

```
🔄 **OpenClaw: доступна версия {latest}**
Текущая: {current} → Новая: {latest} | {releases} релизов

🔑 **Ключевые изменения:**
- {топ 5 самых важных фич/фиксов — кратко, 1 строка каждый}

{если есть breaking changes:}
⚠️ **Breaking changes:**
- {каждый breaking change}

{если есть security fixes:}
🔒 **Security:**
- {ключевые security фиксы}

📋 Полный changelog: https://github.com/openclaw/openclaw/blob/main/CHANGELOG.md

Обновить? Ответь «обнови» или «update».
```

Отправь через message tool:

```
message send --to tg:685668909 --content "<сообщение>"
```

**Правила анализа changelog:**

1. Выбирай фичи, которые реально затрагивают пользователя (Telegram, агенты, модели, cron, iOS)
2. Security fixes — всегда упоминай
3. Breaking changes — всегда упоминай, они требуют внимания
4. Не перечисляй всё — только топ-5 по важности
5. Формулируй кратко: «Slack streaming» а не «add native single-message text streaming with Slack chat.startStream/appendStream/stopStream»

## Шаг 3: Обновление (после одобрения пользователя)

Когда пользователь ответит «обнови», «update», «да, обнови», «go» — выполни обновление.

### Определи среду выполнения:

**Если глобальная npm-установка** (сервер/VPS):

```bash
sudo npm i -g openclaw@latest
```

Затем перезапусти gateway:

```bash
pkill -9 -f openclaw-gateway || true
nohup openclaw gateway run --bind loopback --port 18789 --force > /tmp/openclaw-gateway.log 2>&1 &
```

**Если git-checkout** (dev машина):

```bash
bash scripts/update-openclaw.sh
```

### После обновления:

1. Проверь новую версию: `openclaw --version`
2. Проверь статус: `openclaw channels status --probe`
3. Отправь результат в Telegram:

```
✅ **OpenClaw обновлён!**
{old_version} → {new_version}

Статус gateway: {ok/error}
Каналы: {probe result}
```

Если обновление не удалось — отправь ошибку и предложи помощь:

```
❌ **Ошибка обновления OpenClaw**
{описание ошибки}

Попробуй: `sudo npm i -g openclaw@{latest}` вручную.
```

## Шаг 4: Запись в память

После успешной проверки (независимо от результата) обнови файл `data/update-state.json`:

```json
{
  "lastCheck": "2026-02-19T09:00:00Z",
  "current": "2026.2.17",
  "latest": "2026.2.17",
  "status": "up-to-date",
  "lastUpdate": "2026-02-19T03:00:00Z"
}
```

Если было обновление — также запиши в `memory/YYYY-MM-DD.md`:

```
## OpenClaw Update
Обновлён с {old} до {new}. Ключевые изменения: {список}.
```

## Настройка cron job

Для активации ежедневной проверки создай cron job:

```
openclaw cron add \
  --name "update-checker" \
  --schedule "0 9 * * *" \
  --prompt "Запусти скилл update-checker. Проверь наличие обновлений OpenClaw и если есть новая версия — отправь уведомление в Telegram (tg:685668909). Если обновлений нет — ответь HEARTBEAT_OK, не отправляй ничего в Telegram." \
  --thinking low
```

Перед созданием — проверь `openclaw cron list` на наличие существующего job с именем `update-checker`. Если есть — используй `openclaw cron edit`.

## Обработка команды /update-checker

При получении `/update-checker` от пользователя — запусти полную проверку (Шаг 1 → Шаг 2b) и всегда отправь результат, даже если обновлений нет.
